{
  "name": "agendacelushop",
  "version": "1.0.0",
  "description": "Agenda CeluShop",
  "main": "index.js",
  "scripts": {
    "start": "node backend/server.js",
    "build": "npm install --prefix frontend && npm run build --prefix frontend",
    "dev": "concurrently \"cross-env PORT=3001 npm run start --prefix backend\" \"npm start --prefix frontend\"",
    "heroku-postbuild": "npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2",
    "cross-env": "^7.0.3"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "mongoose": "^9.0.0",
    "serverless-http": "^4.0.0"
  }
}
